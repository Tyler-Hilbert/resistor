#include <iostream>
#include <string>

using namespace std;

/**
	Uncomment this for use case

int main(int argc, const char** argv) {
	enum Color { Black, Brown, Red, Orange, Yellow, Green, Blue, Violet, Grey, White, Gold, Silver };
	string resistor = "";
	Color c = Red;
	
	switch (c) {
	case Black:
		resistor += "bl";
	default:
		resistor += "xx";
	}
	
	cout << resistor;

	system("pause");
	return 0;
}
**/